<Page x:Class="RevitAddIn1.Views.Page1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RevitAddIn1.Views"
      xmlns:viewModels="clr-namespace:RevitAddIn1.ViewModels"
      xmlns:ui="http://lookupengine.com/xaml"
      mc:Ignorable="d" 
      ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
      ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      d:DataContext="{d:DesignInstance viewModels:Page1ViewModel, IsDesignTimeCreatable=False}"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page1">
    <Grid>
        <ui:Button Grid.Row="1" Margin="0,10,0,0" Content="Confirm" Command="{Binding ConfirmCommand}"/>
    </Grid>
</Page>
